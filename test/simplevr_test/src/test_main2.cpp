#define CATCH_CONFIG_MAIN
#include "catch.hpp"

#include <blockscollection.h>

#include <vector>

TEST_CASE("blocks collection makes blocks duh", "[aieee]")
{
    std::vector<unsigned int> vertices;
    std::vector<unsigned short> indices;
    int num_slices = 0;

    SECTION( "Collection" ) {

    }
}

